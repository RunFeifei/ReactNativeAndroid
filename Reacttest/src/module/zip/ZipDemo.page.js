import React, {Component} from 'react';import {Dimensions, Text, View} from 'react-native';import {unzip} from 'react-native-zip-archive'import {ExternalStorageDirectoryPath} from 'react-native-fs'const {height: SCREEN_HEIGHT, width: SCREEN_WIDTH} = Dimensions.get('window');export default class ZipDemo extends Component {    constructor(props) {        super(props);    }    unZip = () => {        const sourcePath = `${ExternalStorageDirectoryPath}/zips/skin.zip`        const targetPath = `${ExternalStorageDirectoryPath}/zips/skin/`        unzip(sourcePath, targetPath)            .then((path) => {                console.log(`unzip completed at ${path}`)            })            .catch((error) => {                console.log(error)            })    }    render() {        return (            <View style={{flex: 1, height: SCREEN_HEIGHT, backgroundColor: 'white'}}>                <Text                    onPress={() => this.unZip()}                >                    解压                </Text>            </View>        );    }}