import React, {Component} from 'react';import {Dimensions, Text, View} from 'react-native';import ToastForAndroid from '../../component/ToastForAndroid';const {height: SCREEN_HEIGHT, width: SCREEN_WIDTH} = Dimensions.get('window');export default class Toast extends Component {    static navigationOptions = ({navigation}) => ({        title: 'Toast'    });    constructor(props) {        super(props);    }    render() {        let toastShort = (            <Text                onPress= {()=>ToastForAndroid.show('ToastShort', ToastForAndroid.LONG)}            >                ToastShort            </Text>        );        let toastLong = (            <Text                onPress= {()=>ToastForAndroid.show('ToastLong', ToastForAndroid.LONG)}            >                ToastLong            </Text>        );        return (            <View style={{flex: 1, height: SCREEN_HEIGHT, backgroundColor: 'white'}}>                {                    [toastShort, toastLong]                }            </View>        );    }}