import React, {Component} from 'react';import {StatusBar, TouchableOpacity, View,} from 'react-native';import Theme, {createStyle, createTheme, createThemedComponent} from 'react-native-theming';import {themes} from '../../App';const Bar = createThemedComponent(StatusBar, ['barStyle', 'backgroundColor']);export default class ThemeDemo extends Component {    render() {        return (            <Theme.View style={styles.container}>                <Bar barStyle="@statusBar" backgroundColor="@backgroundColor"/>                <Theme.Image source="@icon"/>                <Theme.Text style={styles.welcome}>                    TEST                </Theme.Text>                <View style={{flexDirection: 'row'}}>                    {themes.map(theme => (                        <Theme.Text                            key={theme.name}                            onPress={() => theme.apply()}                            style={[styles.buttonText, {color: '@buttonText'}]}>{theme.name}</Theme.Text>                    ))                    }                </View>            </Theme.View>        );    }}const styles = createStyle({    container: {        flex: 1,        justifyContent: 'center',        alignItems: 'center',        backgroundColor: 'white',    },    welcome: {        fontSize: 20,        textAlign: 'center',        margin: 10,        color: '@textColor',    },    button: {        flex: 1,        margin: 10,        padding: 10,        borderRadius: 3,        backgroundColor: '@buttonColor',        alignItems: 'center',    },    buttonText: {        fontSize: 14,        flex: 1,        margin: 10,        padding: 10,        borderRadius: 3,        backgroundColor: '@buttonColor',        alignItems: 'center',    },});