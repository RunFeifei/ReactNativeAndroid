import React, {Component} from 'react';import {View,Image} from 'react-native';import Theme, {createStyle, createTheme} from './lib/index';import {ExternalStorageDirectoryPath} from "react-native-fs";const defaultSkin = {    icon: {uri: `file://${ExternalStorageDirectoryPath}/zips/code.jpg`},    textColor: 'grey',}const themes = [    createTheme(        defaultSkin        , 'Default'),    createTheme({        icon: require('../../imgs/icon-info-red-32.png'),        textColor: 'red',    }, 'Skin'),];export default class ThemeDemo extends Component {    constructor(props, context) {        super(props, context);    }    render() {        return (            <Theme.View style={{                flex: 1,                justifyContent: 'center',                alignItems: 'center',                backgroundColor: 'white'            }}>                <Theme.Image style={{width:100,height:100}} source="@icon"/>                <View style={{flexDirection: 'row'}}>                    {themes.map(theme => (                        <Theme.Text                            key={theme.name}                            onPress={() => theme.apply()}                            style={[styles.buttonText, {                                fontSize: 14,                                alignItems: 'center',                                flex: 1,                                margin: 10,                                padding: 10,                                borderRadius: 3                            }]}>{theme.name}</Theme.Text>                    ))                    }                </View>            </Theme.View>        );    }}const styles = createStyle({    buttonText: {        backgroundColor: '@textColor',    },});