'use strict';import React, {Component} from 'react';import {Text, View,} from 'react-native';import theme, {styles} from 'react-native-theme';theme.add({    container: {        flex: 1,        backgroundColor: 'yellow'    },    title: {        fontSize: 20,        color: 'black',    },});theme.add({    container: {        flex: 1,        backgroundColor: 'green'    },    title: {        fontSize: 20,        color: 'red',    },}, 'red');export default class ThemeDemo extends Component {    componentWillMount() {        theme.setRoot(this)    }    _defaultTheme = () => {        if (theme.name !== 'default') {            theme.active();        }    }    _redTheme = () => {        if (theme.name !== 'red') {            theme.active('red');        }    }    render() {        return (            <View style={styles.container}>                <Text style={styles.title}>SIMPLE</Text>                <Text onPress={() => this._defaultTheme()}>DEFAULT THEME</Text>                <Text onPress={() => this._redTheme()}>RED THEME</Text>            </View>        );    }};